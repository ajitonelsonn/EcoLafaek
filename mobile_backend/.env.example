# =============================================================================
# EcoLafaek Mobile Backend - Environment Configuration Template
# AWS AI Agent Global Hackathon
# =============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
PORT=8000
API_SECRET_KEY=your_secure_random_secret_key_here_change_this
ENVIRONMENT=production
# ENVIRONMENT=development  # Uncomment for local development

# -----------------------------------------------------------------------------
# CORS Configuration
# Comma-separated list of allowed origins
# For production, specify exact domains. For development, use "*"
# -----------------------------------------------------------------------------
ALLOWED_ORIGINS=https://www.ecolafaek.com,https://ecolafaek.com

# -----------------------------------------------------------------------------
# AWS Bedrock Configuration
# Required for Amazon Bedrock Nova-Pro LLM and AgentCore
# -----------------------------------------------------------------------------
# Get these from AWS IAM Console
AWS_ACCESS_KEY_ID=AKIAIOSF7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUENG/bPxRfiCYEXAMPLEKEY
AWS_REGION=us-east-1

# Amazon Bedrock Model Configuration
BEDROCK_MODEL_ID=amazon.nova-pro-v1:0

# -----------------------------------------------------------------------------
# Amazon S3 Configuration
# Required for storing report images and generated charts
# -----------------------------------------------------------------------------
S3_BUCKET_NAME=your-ecolafaek-bucket-name

# -----------------------------------------------------------------------------
# Database Configuration
# Database connection settings (any SQL database compatible with MySQL connector)
# -----------------------------------------------------------------------------
DB_HOST=your-database-host.example.com
DB_NAME=db_ecolafaek
DB_USER=your_database_username
DB_PASSWORD=your_secure_database_password
DB_PORT=4000

# -----------------------------------------------------------------------------
# JWT Authentication Configuration
# Used for mobile app user authentication
# -----------------------------------------------------------------------------
# Generate a secure random key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your_jwt_secret_key_change_this_to_random_string
JWT_EXPIRATION_HOURS=24

# -----------------------------------------------------------------------------
# Email Configuration (Optional)
# Used for sending OTP codes during registration
# If using Gmail, you need to create an App Password:
# https://support.google.com/accounts/answer/185833
# -----------------------------------------------------------------------------
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password_16_characters
EMAIL_SERVER=smtp.gmail.com
EMAIL_PORT=587

# -----------------------------------------------------------------------------
# AgentCore Configuration (Optional - for reference)
# These are configured in .bedrock_agentcore.yaml
# -----------------------------------------------------------------------------
# AGENTCORE_ARN=arn:aws:bedrock-agentcore:us-east-1:YOUR_ACCOUNT_ID:runtime/your_agent_name
# AGENTCORE_ECR_REPOSITORY=YOUR_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/bedrock-agentcore-your_agent

# =============================================================================
# Security Notes:
# =============================================================================
# 1. NEVER commit the .env file to git (it's in .gitignore)
# 2. Use strong, random keys for API_SECRET_KEY and JWT_SECRET
# 3. Rotate credentials regularly
# 4. Use AWS IAM roles instead of access keys when possible
# 5. Store production secrets in AWS Secrets Manager or Parameter Store
# =============================================================================

# =============================================================================
# AWS Setup Instructions:
# =============================================================================
# 1. Create AWS Account and enable Amazon Bedrock in us-east-1
# 2. Request access to amazon.nova-pro-v1:0 model in Bedrock console
# 3. Create IAM user with these permissions:
#    - AmazonBedrockFullAccess
#    - AmazonS3FullAccess (or restricted to your bucket)
#    - AmazonEC2ContainerRegistryFullAccess (for AgentCore)
#    - CloudWatchLogsFullAccess (for monitoring)
# 4. Create S3 bucket for storing images and charts
# 5. Install AgentCore CLI: pip install bedrock-agentcore
# 6. Deploy AgentCore: agentcore launch
# =============================================================================
